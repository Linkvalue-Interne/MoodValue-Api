default:
    translation:
        locale: en
    suites:
        domain_features:
            paths:    [ '%paths.base%/tests/features' ]
            contexts: [ 'MoodValue\Behat\ApplicationContext' ]
